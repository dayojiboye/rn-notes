export enum themeMode {
	LIGHT = "LIGHT",
	DARK = "DARK",
}

export enum toastType {
	ERROR = "ERROR",
	SUCCESS = "SUCCESS",
}

export enum profileItemsEnum {
	AVATAR = "AVATAR",
	DISPLAY_NAME = "DISPLAY_NAME",
}
